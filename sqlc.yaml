version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/db/query"
    schema: "schema/schema.sql"
    gen:
      go:
        package: "db"
        out: "internal/db/sqlc"
        emit_json_tags: true
        emit_interface: true
        emit_exact_table_names: false