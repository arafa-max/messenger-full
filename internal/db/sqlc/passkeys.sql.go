// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: passkeys.sql

package db

import (
	"context"

	"github.com/google/uuid"
)

const createPasskey = `-- name: CreatePasskey :one
INSERT INTO passkeys (user_id, credential_id, public_key, aaguid, sign_count, name)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, user_id, credential_id, public_key, aaguid, sign_count, name, created_at, last_used_at
`

type CreatePasskeyParams struct {
	UserID       uuid.UUID `json:"user_id"`
	CredentialID string    `json:"credential_id"`
	PublicKey    []byte    `json:"public_key"`
	Aaguid       []byte    `json:"aaguid"`
	SignCount    int64     `json:"sign_count"`
	Name         string    `json:"name"`
}

func (q *Queries) CreatePasskey(ctx context.Context, arg CreatePasskeyParams) (Passkey, error) {
	row := q.db.QueryRowContext(ctx, createPasskey,
		arg.UserID,
		arg.CredentialID,
		arg.PublicKey,
		arg.Aaguid,
		arg.SignCount,
		arg.Name,
	)
	var i Passkey
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.CredentialID,
		&i.PublicKey,
		&i.Aaguid,
		&i.SignCount,
		&i.Name,
		&i.CreatedAt,
		&i.LastUsedAt,
	)
	return i, err
}

const deletePasskey = `-- name: DeletePasskey :exec
DELETE FROM passkeys WHERE id = $1 AND user_id = $2
`

type DeletePasskeyParams struct {
	ID     uuid.UUID `json:"id"`
	UserID uuid.UUID `json:"user_id"`
}

func (q *Queries) DeletePasskey(ctx context.Context, arg DeletePasskeyParams) error {
	_, err := q.db.ExecContext(ctx, deletePasskey, arg.ID, arg.UserID)
	return err
}

const getPasskeyByCredentialID = `-- name: GetPasskeyByCredentialID :one
SELECT id, user_id, credential_id, public_key, aaguid, sign_count, name, created_at, last_used_at FROM passkeys WHERE credential_id = $1
`

func (q *Queries) GetPasskeyByCredentialID(ctx context.Context, credentialID string) (Passkey, error) {
	row := q.db.QueryRowContext(ctx, getPasskeyByCredentialID, credentialID)
	var i Passkey
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.CredentialID,
		&i.PublicKey,
		&i.Aaguid,
		&i.SignCount,
		&i.Name,
		&i.CreatedAt,
		&i.LastUsedAt,
	)
	return i, err
}

const getPasskeysByUserID = `-- name: GetPasskeysByUserID :many
SELECT id, user_id, credential_id, public_key, aaguid, sign_count, name, created_at, last_used_at FROM passkeys WHERE user_id = $1 ORDER BY created_at DESC
`

func (q *Queries) GetPasskeysByUserID(ctx context.Context, userID uuid.UUID) ([]Passkey, error) {
	rows, err := q.db.QueryContext(ctx, getPasskeysByUserID, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Passkey
	for rows.Next() {
		var i Passkey
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.CredentialID,
			&i.PublicKey,
			&i.Aaguid,
			&i.SignCount,
			&i.Name,
			&i.CreatedAt,
			&i.LastUsedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updatePasskeySignCount = `-- name: UpdatePasskeySignCount :exec
UPDATE passkeys
SET sign_count = $2, last_used_at = NOW()
WHERE credential_id = $1
`

type UpdatePasskeySignCountParams struct {
	CredentialID string `json:"credential_id"`
	SignCount    int64  `json:"sign_count"`
}

func (q *Queries) UpdatePasskeySignCount(ctx context.Context, arg UpdatePasskeySignCountParams) error {
	_, err := q.db.ExecContext(ctx, updatePasskeySignCount, arg.CredentialID, arg.SignCount)
	return err
}
